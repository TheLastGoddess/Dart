void main(List<String> args) {
  var record = ('HEllo', a: 2, 23, 'dkljkl');
  print(record.a);
  list(List<double> number) {
    return (number.first, number.length);
  }

  print(list([2.3, 13, 35, 2.4]));
}
